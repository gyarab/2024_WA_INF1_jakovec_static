export function createCamera(){
    return { x: 0, y: 0 };
}
export function createZoomValues(){
    return {
        gridScale: 1,
        zoomSpeed: 0.1,
        minScale: 0.2,
        maxScale: 2.5,
    }
}